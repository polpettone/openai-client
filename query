type Entry struct {
	value  string
	tokens int
}

type ContextMemory struct {
	buffer    []*Entry
	maxTokens int
	id        string
}

Baue eine golang funktion um ContextMemory in eine file als json to speichern.
Das Speicher in die File soll die File erweitern und nicht truncaten.
